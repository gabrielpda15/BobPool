import config from './config.json';
import readme from './readme.json';
import rules from './rules.json';
import blocked from './blocked.json';
import stream from './stream.json';

export default { config: config, readme: readme, rules: rules, blocked: blocked, stream: stream };
